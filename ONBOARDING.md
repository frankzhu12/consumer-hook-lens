# 新人上手：第 1 小时 · 和带回家的那件事

写给刚拿到这个仓库的人。看完这一页就能跑起来，并且**带走一件真的东西**。

先读 [README.md](README.md)（怎么跑起来 / 怎么跑测试）。
这一页不重复它的内容 —— 同一件事写两遍，就一定会有一遍忘了改。

---

## 一、第 1 小时（分钟级时间盒）

目标**不是**「看一遍代码」，是带走三样东西：
一个能跑的环境、一次亲手弄坏再修好的经历、一份界面验收清单。

| 时间 | 做什么 | 这一段的产出 |
|---|---|---|
| 0–10 min | **只读，不写代码**：README 全文 → PRD 的 §5（五屏流程）和 §6（词典表） | 能用自己的话说出「教 → 练 → 放手」各是什么 |
| 10–25 min | 把工程跑起来：开发者工具导入 → 五屏各走一遍 | 环境能跑 |
| 25–40 min | 跑测试，然后**故意弄坏一个东西** | 一份「我弄坏了什么 → 哪一条变红」的记录 |
| 40–55 min | 按清单手点验收阶段 3–8 的视觉部分 | 一份问题清单（没有就写「没有」） |
| 55–60 min | 写报告（模板在最后） | 报告 |

### 10–25 min：把工程跑起来

按 README「怎么跑起来」那一节做。两点说明：

- `project.config.json` 里的 appid 是**这个项目作者的**。你导入后会报错说它不属于你的账号 ——
  改成你自己的，或改成 README 里写的那个游客占位值走游客模式。**游客模式足够做完今天这件事。**
- 五屏**一定要自己手点一遍**：示例 1（点图上的消费陷阱，卡片才上线）→ 示例 2（两处都自己找）→
  示例 3（一处都不给）→ 按住「只看商品」→ 生成卡片。

### 25–40 min：把测试套件当成你的老师

```
node tests/run-all.js
```

判断标准：**每一行末尾都显示 `0 失败`**。
（不要去看「一共几项」——那个数字每加一个测试文件就过期，所以它故意不写进任何地方。）

然后做这件更重要的事：**故意弄坏一个东西，看哪条断言变红。** 例如

- 把 `utils/reveal.js` 里 `initialRevealed` 的返回值从 `[]` 改成 `[hooks[0].id]`（恢复预制）→ 应该有断言变红
- 把 `utils/hooks.js` 里某个维度的 `closing` 改一个字 → 应该有断言变红

改完**一定要还原**，再跑一遍确认回到全绿。

这一步的目的只有一个：**让你相信这套测试是真的会咬人的。**
一个不咬人的绿色检查，比没有检查更危险。

### 40–55 min：手点验收 —— 这件事只有人能替机器做

这个项目有一条分工：**有界面的手点，没界面的必须写脚本。**

所以脚本测不到的东西被明确列了出来，其中**阶段 3–8 的视觉部分要靠人过一遍**，这轮还欠着 ——
你花 15 分钟就能补上，而且这是整个项目里**搬得快、又没人抢**的一件事。

逐条看，记录「现象 / 在哪一屏 / 你预期是什么」：

- 标注框是否框在**正确的元素**上？（不要求像素级精确，要求「框住的就是它说的那个东西」）
- 编号（1 / 2）和底部卡片的顺序对得上吗？
- 按住「只看商品」时：促销元素消失，**商品和当前价格必须留下**；松手要完全还原
- 按住时图上应该是干净的 —— 没有任何横幅或小字压在上面（横幅已拆，别让它在验收时复活）
- 卡片页：截图、标注框、陷阱名清单、一句结论，四样都在吗？
- 转发出去，微信消息列表里的缩略图好看吗？（微信只取顶部 5:4，长卡片会被裁）

### 55–60 min：报告

```
环境：能跑 / 卡在哪一步、报什么错
测试：全绿 / 哪一条红、为什么
我弄坏的：改了什么 → 哪条断言变红
界面验收：发现的问题（没有就写「没有」）
```

---

## 二、看起来像 bug、其实是设计

**动手前先看这一节。** 下面这些第一眼很像 bug，但改掉会把演示弄坏。

| 现象 | 为什么是故意的 | 在哪 |
|---|---|---|
| **示例 2 进屏时，一处都不预先标出** | 产品决定：三段示例统一「先点图、卡片才上线」，第二屏不再预制第一处（历史上曾预制，`initialRevealed` 保留了唯一出口）。三档 `mode`（reveal / find-one / find-all）仍写进数据里 —— 找的模式下没找到的不露名字，这段骨架没动 | `utils/reveal.js` 的 `initialRevealed`。`tests/check-reveal.js` 有断言锁着「进屏什么都不预给」 |
| 示例 2 / 3 里，**没找到的那几处看不到名字** | 剧透了就没得找了 | `utils/reveal.js` 的 `shouldHideName` |
| 找的模式下**点底部卡片没反应**，只弹一句「先在图里找找看」 | 故意点不动 —— 否则一路点过去就把「找」这件事绕过去了。「直接显示」才是合法退路 | `pages/journey/index.js` 的 `onCardTap` |
| **按住时点哪儿都不响应** | 一只手按住、另一只手改状态会让状态错乱，所以每个处理函数开头都有一句 `if (this.holding) return;` | 同上 |
| 除了产品名，还有 `CARD_FOOTER` / `SHARE_TITLE` 两个字符串 | 它们只在**分享出去的图片和消息**里出现，不在小程序界面上 | `utils/brand.js` |

> 这个项目有一条硬规矩：**同一件事只写一遍。**
> 陷阱名和机制句只活在 `utils/hooks.js`；产品名只活在两处（`utils/brand.js` + `app.json`）。
> 你加东西的时候如果发现「必须写第二遍」，通常说明放错了地方 —— 先回去看 README 里那两张表。

---

## 三、带回家的那件事：给转发卡片做一张 5:4 的缩略图

### 为什么是它

90 秒演示脚本的 70–85 秒是「生成卡片并转发」。但微信在消息列表里只取**顶部 5:4** 那一块当缩略图，
而我们的卡片接近 1:2（截图很高）—— 所以转发出去大概只能看到截图上半部分，**很可能看不到标注框**。
这是演示里少数几个「可能不好看」的地方，而且是**能修的**。

### 要做成什么样

生成卡片时**多产出一张 5:4 的图**，专门给转发用；**卡片本身（页面上展示的那张）版式不许改。**

- 5:4 缩略图里必须能看到：截图的顶部区域 + **至少一个标注框**
- 卡片页现在的样子，一个字都不改

### 动手的位置

| 文件 | 做什么 |
|---|---|
| `utils/card.js` | 新加一个**纯布局**函数（只产出绘制计划，不碰 canvas）。这是唯一正确的位置 —— 排版和渲染必须分开，理由见文件顶部注释 |
| `utils/canvas-draw.js` | 大概率不用改，直接复用 `drawPlan` |
| `pages/card/index.js` | 照现有 `paint()` 的套路再产出一张；`onShareAppMessage` 的 `imageUrl` 指向它 |
| `tests/check-card.js` | 加断言（见下） |

### 验收标准（必须能自动成立）

改完跑 `node tests/run-all.js`，**每一行末尾都显示 `0 失败`**。另外至少要有这几条新断言：

1. 缩略图的宽高比是 **5:4**
2. 缩略图里**至少有一个标注框**落在画布范围内（没被裁掉）
3. 传空数据 / 传一张「一处都没标注」的卡片时**不崩**，且返回明确的失败，而不是一张空白图
4. 两张图的绘制计划**互不影响**（改缩略图不会动到主卡片）

> 写断言前先读 `utils/card.js` 和 `tests/check-card.js` 顶部那几段注释。
> 另外：**新写的断言要自己做一次变异测试** —— 故意让它失败一次，确认它真的会咬人，再还原。

### 只能真机确认的部分（做完单独写一行）

缩略图在微信消息列表里的**实际观感**，只能在真机上转发一次才知道。
脚本能证明「画布比例对了、标注框在范围内」，证明不了「好不好看」。
这两件事别混为一谈，报告里分开写。

### 别做的事

- ❌ 不要改整张卡片的版式去迁就 5:4。卡片是给人**点开看**的，缩略图只是入口。
- ❌ 不要动 `utils/card.js` 里现有的 `buildCard` 行为 —— 主卡片已经被断言锁住了。
- ❌ TODO.md 末尾「明确不做」那一节列的东西（登录、支付、列表页、历史记录……）一个都不要加。

---

## 四、如果你是从作者的仓库 fork 过来的

前三节对任何人都适用，这一节只讲 fork 之后**必须自己决定/自己配**的那几件事。

### 0. 明天就要跑起来：照这五步做

同一个 appid 的情况下，**要配的东西一件都没有** —— 环境、云函数、环境变量都已经在云端了。
所以明天的动作是「拉代码 → 编译 → 手点」，不是「配环境」：

| # | 做什么 | 做完该看到什么 |
|---|---|---|
| 1 | 从上游拉最新代码（`git pull upstream main`） | 最新一条提交是「…像素坐标…解析器要能换算」 |
| 2 | `node tests/run-all.js` | 每一行末尾 `0 失败` |
| 3 | 微信开发者工具导入项目，**编译** | 控制台没有 Syntax Error（报在测试文件上就去看 `packOptions.ignore`） |
| 4 | 前三屏手点一遍：示例 1 → 2 → 3 → 按住「只看商品」→ 生成卡片 | 这部分**完全离线**，不需要云 |
| 5 | 进第四屏，选一张自己的购物截图 | 出 2–3 个框，引导条说「它读出了 N 处 · 点图上的框，看每一处是什么」 |

第 5 步万一显示「有几处它不太确定，先不标了」，**先看控制台那行 `[journey] 标注被丢掉：…`** ——
那里写着是哪一条被丢了、因为什么（`bad-rect` / `low-confidence` / `unknown-pattern`）。
别急着改阈值，把那一行贴给作者。

> ⚠️ **不要部署云函数。** 云函数由一个人负责部署，两人先后部署同一个 `detect`，
> 后一次会静默覆盖前一次。你要改 `cloudfunctions/` 里的东西，改完交给那个人点一次。

### 1. 先确认一件事：你的 appid 和作者是不是同一个

```
node -e "console.log(require('./project.config.json').appid)"
```

**一样 → 你们在共同开发同一个小程序**，看 A。
**不一样 → 你另起了一个小程序**，看 B。

#### A. 同一个 appid：什么都不用配

云开发环境是**跟着 appid 走的** —— 同一个小程序，你和作者看到的本来就是同一个环境。
`app.js` 里的 `CLOUD_ENV` 已经填好了，作者部署过的 `detect` 云函数、以及它配好的环境变量，
都在那儿。你 clone 下来**直接就能跑第四屏**。

所以这里要守的不是"怎么配"，而是**两个人别互相踩**：

| 规矩 | 为什么 |
|---|---|
| **云函数只由一个人部署** | 两个人先后部署同一个 `detect`，后一次会**静默覆盖**前一次 —— 被覆盖的人收不到任何提示，只会看到「我明明改了，怎么没变」。想改云函数：改完代码，交给负责部署的那个人点一次 |
| **三个环境变量只有一份** | 环境变量是**函数级**的，不是人级的。所以 `MODEL_API_KEY` 用谁的、百炼额度算谁的，要提前说好。两个人各配一次的话，后配的覆盖先配的 |

> 顺带澄清一个容易搞混的概念：云开发的**「环境共享」是给同一主体下的不同小程序用的**，
> 同一个 appid 根本用不到它；而不同主体的两个小程序**想用也用不了**。别在这上面花时间。

#### B. 不同的 appid：自己开一个环境

（这个仓库的 appid 是作者的 —— 另起炉灶就改成你自己的，或改成游客占位值。
**游客模式足够看完前三屏和卡片页**，那部分完全离线。）

1. 开通云开发，**基础版/免费额度就够** —— 不需要标准版，那条门槛已经绕开了；
2. 部署 `cloudfunctions/detect`（右键 → 上传并部署）；
   ⚠️ 部署完**去控制台核对函数超时是 60 秒** —— `config.json` 里的值经常不生效，
   会静默回落到默认的 3 秒；
3. 在该云函数的「配置 → 环境变量」里配三个：

   ```
   MODEL_BASE_URL   端点前缀（OpenAI 兼容的 /v1 那一级）
   MODEL_API_KEY    Bearer 令牌 —— 三个里唯一敏感的一项
   MODEL_NAME       模型名，**必须支持图片输入**
   ```

   第四个是可选的：`MODEL_REASONING_EFFORT`，默认 `none`（关思考）。
   只有换到**不吃这个参数**的模型时才需要配 —— 配成空串，请求体里就不会带上这个字段。
   关思考这条别改：实测同一个模型不开思考 18.1 秒、开了 2.5 秒，
   云函数里那两次「20 秒整、0 字节」就是思考拖的。

   > Key 只存在于**控制台的环境变量**里：不进代码、不进 `config.json`、不进文档。
   > 换 Key 只需在控制台改一次，不用重新部署。

4. 把 `app.js` 里的 `CLOUD_ENV` 换成**你自己那个环境的 ID** —— 只改这一处，
   也别把它写进任何文档。

### 2. 跑测试

```
node tests/run-all.js
```

判据：**每一行末尾都显示 `0 失败`**（不要看"一共几项"，那个数字会变）。
测试文件放在 `tests/check-*.js` 会被自动收进去，不用登记。

改了 `utils/hooks.js`（词典）之后**必须**跑一次：

```
node tools/build-detect-prompt.js
```

—— 云函数侧有一份生成出来的副本（云函数打包不到上级目录，只能落盘一份），
不重新生成的话 `check-detect-sync` 会变红。

### 3. 跟着上游更新

```bash
git remote add upstream https://github.com/<作者>/consumer-hook-lens.git
git pull upstream main
```

### 4. 两处别写进文档

**appid 和云环境 ID 是会变的值**：只在配置文件里写一遍，文档一律指路不复述。
`tests/check-docs.js` 专门守着这一条 —— 它会变红，不是它太严格，是你真的会忘。
