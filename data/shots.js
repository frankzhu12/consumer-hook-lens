/**
 * 三段示例的数据。
 *
 * 这里**只写位置，不写文案**：
 * 陷阱名和机制句一律从 utils/hooks.js 取（唯一来源）。
 * 改文案 → 改词典；改位置 → 改这里。两件事不要混在一个文件里。
 *
 * rect 是相对整张图的 0–1 归一化比例（左上原点），
 * 数值是用标尺裁剪核对过的，不是"看着差不多"填的。
 *
 * 场景（与主页选图卡的三个标签对应）：
 * 01 电商商品页（购物）→ 02 支付确认页（支付）→ 03 红包活动页（活动）。
 * 钩子类型按 hooklens 的五维词典辨认：A 紧迫 / B 价格感知 / C 阻力消除 /
 * D 社交从众 / E 进出不对称。
 */

const SHOTS = [
  {
    id: 'shot-01',
    // 教：卡片上写着陷阱名，点一下看它在哪
    mode: 'reveal',
    appName: '电商商品页',
    image: '/assets/samples/01.jpg',
    hooks: [
      // 限时秒杀横幅（含 00:14:59 倒计时），圆角横幅几乎全宽。
      // 左缘不贴 0：卡片页的标签排版要求框离边至少一档
      { id: 'A1', rect: { x: 0.015, y: 0.429, w: 0.97, h: 0.048 } },
      // 只框划线价那一小段，不框当前价 ——「按住」时划线价要消失，当前价格必须留下，
      // 那个对比（原价没了、现价还在）才是这个陷阱被读懂的一刻。
      // 边界按标尺裁剪量的：¥599 占 x 0.290–0.408、y 0.515–0.535（原图 862×1824），
      // 左边界避开左边的现价 ¥299（到 x≈0.278 为止），右边界避开「直降300元」胶囊。
      { id: 'B1', rect: { x: 0.29, y: 0.512, w: 0.125, h: 0.026 } }
    ]
  },
  {
    id: 'shot-02',
    // 练：不再预制 —— 两处都自己在图上点出来（曾先替你揭示凑单条，已统一）
    mode: 'find-one',
    appName: '支付确认页',
    image: '/assets/samples/02.jpg',
    hooks: [
      // 保障服务块：延长保修 / 优先发货两行都替你打好勾（默认勾选与搭售）
      { id: 'C2', rect: { x: 0.02, y: 0.315, w: 0.96, h: 0.148 } },
      // 省钱月卡块：首月 ¥9，次月起 ¥19/月自动续费
      { id: 'E2', rect: { x: 0.02, y: 0.679, w: 0.96, h: 0.089 } }
    ]
  },
  {
    id: 'shot-03',
    // 放手：一处都不给，全靠自己找（有「直接显示」兜底）
    mode: 'find-all',
    appName: '红包活动页',
    image: '/assets/samples/03.jpg',
    hooks: [
      // 「本轮福利剩余 00:09:58」倒计时药丸条
      { id: 'A1', rect: { x: 0.22, y: 0.404, w: 0.565, h: 0.037 } },
      // 「小橙刚刚领走 100 元红包」从众提示条
      { id: 'D1', rect: { x: 0.03, y: 0.86, w: 0.935, h: 0.042 } }
    ]
  }
];

module.exports = { SHOTS: SHOTS };
