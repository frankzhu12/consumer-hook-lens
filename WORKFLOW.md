# 开发流程：从零到能演示

照着做就行的一页。每一步都有**验收标准**——看不到那一条，就说明这步没做完。

> 这份文档管「流程」：先做什么、后做什么、做完看什么。
> 命令怎么敲、规矩为什么这么定、下一步做哪个功能，分别归 [README.md](README.md)、[PRD.md](PRD.md)、[TODO.md](TODO.md)——
> 同一件事只写一遍，这里不重复。

---

## 0. 工具与账号（一次性，约 30 分钟）

| 项 | 要什么 | 备注 |
|---|---|---|
| 小程序账号 | 一个 AppID | 正式 AppID 才能用云开发；没有就先走游客模式开发界面 |
| 腾讯云账号 | 与小程序账号可不同 | 云开发环境挂在这里；计费也在这里 |
| 微信开发者工具 | 稳定版 | 导入项目、跑模拟器、部署云函数、真机调试都靠它 |
| Node.js | LTS | 只用来跑测试；不装就没法验证 |
| Git + GitHub 账号 | 一个 | 代码托管；多人协作用两个远端（见第 3 步） |

**两条账号层面的坑，先记住**：

- 云开发「升级套餐」与「切换为资源点计费」是**两个动作**：升级完套餐还要去套餐用量页点「切换为资源点」，否则模型开关点不开；
- 计费单位是**环境**，不是小程序也不是公司：一个环境可以服务多个小程序；只有另开环境才需要再买套餐。

---

## 1. 首次把项目跑起来（约 15 分钟）

1. **拿代码**：从模板仓库 clone 到本地任意目录；
2. **导入**：开发者工具 → 导入项目 → 选这个目录。若提示 appid 不属于你的账号，改成自己的 AppID，或改成游客占位值走游客模式；
3. **看界面**：五屏各手点一遍——示例 1（点图上的消费陷阱，卡片才上线）→ 示例 2（两处都自己找）→ 示例 3（一处都不给）→ 按住「只看商品」→ 生成卡片；
4. **跑测试**：`node tests/run-all.js`。

> **验收**：五屏都能点通，且测试输出**每一行末尾都是 `0 失败`**。
> 注意判据是「0 失败」，不是「一共几项」——项数每加一个文件就过期，所以刻意不写进文档。

5. **建立信任**：故意弄坏一个东西（例如把 `utils/reveal.js` 里 `initialRevealed` 的返回值改成 `[hooks[0].id]`），再跑测试，看哪条断言变红；然后还原、确认全绿。

> **验收**：你能说出「改了什么 → 哪一条变红」。一个不咬人的绿色检查，比没有检查更危险。

---

## 2. 日常开发循环（每天重复）

```
① git pull                     拉最新
② 看 TODO.md                   找「下一步」是哪个编号
③ 让助手改代码                  说清：改哪个文件、验收标准是什么
④ node tests/run-all.js        每行 0 失败
⑤ 有界面的：手点一遍            没界面的：必须写脚本
⑥ 新断言做变异测试              故意让它红一次，再还原
⑦ 提交 + 推送                  见第 3 步
```

**第 ③ 步怎么提要求**（决定这一轮顺不顺）

- 说**验收标准**，不说实现细节：「第四屏失败态要能重试」，而不是「加个 try catch」；
- 一次一步。任务拆小、逐个验收，比一口气做完再调稳得多；
- 想要 UI 效果先要**规格**（尺寸、颜色、文案），再让写代码——不然改三轮都改不到点上。

**第 ⑤ 步的分工**（这是项目的硬规矩）

- **有界面的手点**：视觉、手感、顺序、剪裁，脚本证明不了；
- **没界面的写脚本**：状态机、文案分支、坐标换算，一律脚本化，并**自动发现**（测试文件放在 `tests/check-*.js`，跑全量时自动收）。

**第 ⑥ 步为什么不能省**：断言写错方向（写成永远为真）自己不会报错。让它红一次，才知道它真的在看这件事。

---

## 3. 提交与推送

```bash
git status                                  # 看一眼改了什么
git grep -n -I -E "sk-[A-Za-z0-9]{16,}|ghp_|AKIA[A-Z0-9]{12,}|-----BEGIN"   # 扫密钥
git add -A && git commit -m "阶段 X.Y：做了什么"
git push origin main                        # origin = 自己的仓库
```

- **推之前必须扫密钥**。公开仓库尤其；扫出来的命中要逐条看清是真的还是文档里描述特征的那行字；
- 公司网络里 `git push` 挂住时，通常是代理问题——给 git 指定本机代理再推；
- **两个远端**：`origin` 是你的仓库，另一个人推他自己的。同步对方的版本用 `git pull <对方远端> main`；
- ⚠️ **同步失败时的绕法**（本机实测）：某些工作目录里 git 的 `unlink` 会被拒
  （`unable to unlink old 'PRD.md': Operation not permitted`），`merge` / `reset --hard` /
  `checkout` 全部失败，而 shell 的 `rm` 正常。做法：
  ① 内容冲突手动合（两边改动是**共存**关系，不是二选一）；
  ② 取对方的新文件用 `git checkout <对方远端>/main -- <路径>`（文件不存在时不需要 unlink）；
  ③ 提交时造**双亲合并提交**，让 git 记住已经合过：
  `git add -A` → `git write-tree` → `git commit-tree <tree> -p HEAD -p <对方远端>/main -m "…"` → `git update-ref HEAD <新提交>`。
  少了这一步，下次同步会**重复冲突同一批文件**；
- **仓库里的云函数代码 ≠ 云端在跑的版本**。改了云函数，必须重新部署（见第 4 步第 6 条）。

---

## 4. 云开发与 AI 接入（本项目已跑通，照这个顺序做）

1. **开通云开发**，拿到环境 ID（不是密钥，可以写在客户端）；
2. **把环境 ID 填进客户端**——只填一次，位置在 `app.js` 的 `CLOUD_ENV`，其余地方引用它。
   > ⚠️ 环境 ID **不要写进任何文档**：它属于「会变的值」，文档复述了就一定有人忘了改。
3. **控制台启用模型**：云开发控制台 → AI → 生文模型 → 用「输入类型：图片」或分类「视觉理解」筛出**支持图片输入**的模型（纯文本模型收图会静默失败）→ **打开状态开关**；
   > 免费体验模型只有纯文本混元，读图必须选视觉模型；第三方提供商要标准版套餐。
4. **写云函数**：目录放在 `cloudfunctions/<名字>/`，依赖写进它自己的 `package.json`；
   > 云函数**只打包自己这个目录**，`require` 不到上级的 `utils/`。需要共享的数据用**脚本生成副本**（本项目是 `tools/build-detect-prompt.js`），并写断言防漂——不要手抄。
5. **部署**：开发者工具里右键云函数目录 →「上传并部署：云端安装依赖」；
   > ⚠️ 云函数目录**不能有空目录**，会导致打包失败。
6. **立刻核对两个超时**（最容易白排查半天的一步）：
   - **函数超时**：部署时 `config.json` 里的值**经常不生效**，去控制台函数配置里看，不对就手动改（本项目上限 60 秒）；
   - **SDK 请求超时**：`tcb.init({ timeout })` 默认只有 15 秒，模型读图常常不够，必须显式放宽。
   > 每次重新部署后都回头看一眼这两处——它们是「改了但没生效」的重灾区。
7. **三级验证，从小到大**：

   | 顺序 | 怎么测 | 能证明什么 |
   |---|---|---|
   | ① | 云端测试 + 无效参数 `{"fileID":"test"}` | 函数活着、入口守卫对（应快速返回约定的错误 JSON） |
   | ② | 云端测试 + 真实 `fileID`（先往云存储传一张图，复制其 File ID） | 模型链路通、耗时多少（看 Duration） |
   | ③ | 真机走完整界面流程 | 端到端可用 |

8. **看返回判断问题在哪层**：返回**我们自己约定的 JSON** → 业务侧（看 annotations/失败原因）；返回**平台错误页**（如 `-3 system error`、`-504003`）→ 平台侧（等几十秒重试 / 查超时配置 / 看是不是刚部署完实例没就绪）。

---

9. **两条路，二选一由「有没有配 Key」决定**（代码里两条都在，不需要改代码）：

   | | 云开发内置大模型 | 直连外部模型（自写 HTTPS） |
   |---|---|---|
   | 门槛 | 要**标准版套餐** + 控制台开模型开关 | 只要一个 Key，**基础版环境就能跑** |
   | 耗时 | 实测 25 秒以上 | 实测 3 秒上下 |
   | 凭证 | 平台托管，代码里不用管 | 自己在云函数控制台配 |

   直连这条路要在**云函数 → 配置 → 环境变量**里配三个（`cloudfunctions/detect/lib/model-http.js` 会读）：

   ```
   MODEL_BASE_URL   端点前缀（OpenAI 兼容的 /v1 那一级，尾部斜杠可省）
   MODEL_API_KEY    Bearer 令牌 —— 三个里**唯一敏感**的一项
   MODEL_NAME       模型名
   ```

   > ⚠️ **Key 只存在于控制台的环境变量里**：不进代码、不进 `config.json`、不进文档、不进聊天记录。
   > 环境变量不等于"安全"——有该环境控制台权限的人都能看到明文，它只是保证**不会随代码流传**。
   > 换 Key 只需在控制台改一次，**不用重新部署**。
   >
   > 另外四件实测出来的事：
   > ① **必须支持图片输入的模型**（名字里带 `vl` / `omni`）。纯文本模型收图不会报错，
   >    表现是"云函数成功返回、一条标注都没有"，极容易被误判成"模型看不出问题"；
   > ④ **模型会直接给像素坐标**（`{"x":264,"y":789,...}` 或 `[36,557,283,60]`），
   >    比例值和像素值**随机交替**。判据是"四个数里有一个 > 1"。
   >    不换算的话，36 被夹成 1、283 被夹成 1，宽高压成 0 → 整条判非法 → **整屏 0 条标注**，
   >    界面说"有几处它不太确定，先不标了"——看起来像模型没把握，其实是我们自己扔的。
   >    换算需要图幅，客户端用 `wx.getImageInfo` 取；取不到就照旧判非法（宁可不画，也不画错位）。
   > ② 账号免费额度用尽时返回 **403**，日志里会带上状态码和原文 —— 看到 403 先看额度，别先怀疑代码；
   > ③ **思考型模型必须关思考**（2026-09-25 实测，同一个 qwen3.8-omni-flash、同一张图）：
   >
   >    | | 不带思考参数 | `reasoning_effort: none` |
   >    |---|---|---|
   >    | 耗时 | **18.1 秒** | **2.5 秒** |
   >    | 输出 token | 1372（其中 1199 个在思考） | 182（全是正文） |
   >
   >    非流式请求要等思考**全部生成完**才返回，所以这段时间用户只是干等。
   >    ⚠️ 它的现场表现是「云函数两次都在超时线整秒被打断、**一个字节都没收到**」——
   >    很容易被当成网络问题、出网被墙、Key 失效，其实都不是。
   >    代码里已默认关（`MODEL_REASONING_EFFORT` 留空可关掉这个字段），见 `lib/model-http.js` 文件头。

   **直连这条路的时间预算**：`index.js` 里是 **1 次调用、55 秒**，不再重试。

   > 为什么不重试：重试只对「偶发抖动」有用，对「模型就是慢」没用 —— 慢到 55 秒的情况，
   > 重来一次只是把失败延后。而 20 秒 ×2 那套更糟：绳子正好卡在思考的脖子上，
   > 两次都必然被打断（这就是当时 0 字节的原因）。
   > 请求超时写 55 而不是 60，是因为 60 秒是**平台掐掉函数**的线 —— 卡到那一刻，
   > 函数被杀、日志里一行都没有。留 5 秒收尾，至少能留下「模型请求超时」这句话。

## 5. 真机、体验版、提审

**真机调试前必做**：在 `project.config.json` 的 `packOptions.ignore` 里排除**不参与打包的目录**（`tests/`、`tools/`、云函数目录）。

> 为什么：打包器会把它们也编进去，而测试脚本里有 Node 才允许的写法（顶层 `return`），编译直接报 Syntax Error。**报错指向测试文件时，不要改测试——排除它。**

**三个"真机"的区别**：

| 方式 | 用来干什么 |
|---|---|
| 真机调试 | 看 Console 日志、断点；问题排查 |
| 预览 | 扫个码自己试手感；不占版本 |
| 体验版 | 给评委/同事用；必须在公众平台设置 |

**上线前**：隐私保护指引、服务类目、截图素材都要在公众平台补齐；提交审核后按反馈改，别在审核期反复推版本。

---

## 6. 排错口诀（本项目实际踩过的，按"现象 → 根因 → 解法"）

| 现象 | 根因 | 解法 |
|---|---|---|
| 真机调试编译报 Syntax Error，指向测试文件 | 打包器把 `tests/` 编进了小程序包 | `packOptions.ignore` 排除那些目录 |
| 真机调用报 `Invoking task timed out after 3 seconds`，云端日志空 | 函数实际超时是默认 3 秒（`config.json` 没生效） | 控制台函数配置里改超时（优先级最高） |
| 函数日志里每次调模型都在 ~15 秒报 `ESOCKETTIMEDOUT` | node-sdk 自己的请求超时默认 15 秒 | `tcb.init({ timeout: 55000 })` |
| 调用成功但一条结果都没有 | 模型是纯文本的，图被忽略 | 换成支持图片输入的模型，并确认开关已开 |
| 控制台测试报 `-3 Upstream error` | 平台侧抖动 / 刚部署完实例未就绪 | 等 1–2 分钟重试；或换网页版控制台测试 |
| 模型偶尔很慢（几十秒） | 模型本身耗时 + 网关排队 | 压小图片、换更快的模型；演示用图走**预热缓存** |
| 改了云函数却毫无变化 | 只改了本地代码，没重新部署 | 重新「上传并部署：云端安装依赖」 |

**通用判断顺序**：先看**返回是不是我们约定的结构**（判断平台侧/业务侧）→ 再看**云函数日志的 Duration**（判断慢在哪一段）→ 再看**客户端 Console 的原始错误**（判断请求有没有发出去）。

---

## 7. 五条不许破的规矩

1. **同一件事只写一遍**：会发现「必须写第二遍」时，通常说明放错了位置。会变的值（appid、环境 ID）一律不进文档，指路到配置文件。
2. **有界面的手点，没界面的写脚本**；脚本判据是每行 `0 失败`，且新断言必须做过变异测试。
3. **不假装成功**：模型没读出来就说没读出来。空结果和失败要分成不同的话说清楚。
4. **云函数是 AI 的唯一出口**：密钥、提示词、模型名都在服务端；客户端只负责选图、上传、渲染。
5. **每次改完云函数，重新部署 + 核对超时**。
