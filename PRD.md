# PRD · 收手吧（微信小程序）

- **版本**：v0.1
- **日期**：2026-09-18
- **状态**：待评审（用户确认后才进入开发）
- **一句话**：把手机截图里那些「诱导你多花钱的设计」框出来，并给它一个名字。

---

## 1. 为什么做这个

黑客松参赛作品，**唯一目标是获奖**。因此本文档的取舍标准不是「功能是否完备」，而是「评委的 2 分钟里能否被记住」。

产品立意是**绿色消费**：让人看清自己是怎么被设计成多花钱的。定位上只做「揭示机制」，不做「道德劝说」——这个区别会在文案规范里被写成硬规矩（见 §9）。

**与网页版 HookLens 的关系**：继承其技术内核（分类体系 + 视觉模型识别 + 归一化坐标标注 + 模板兜底），但**产品故事和词典完全重写**，从「注意力成瘾」转向「消费陷阱」。

---

## 2. 成功的定义

| 维度 | 标准 |
|---|---|
| 演示成功 | 评委在 2 分钟内理解产品，并**亲自完成一次**「原来我的注意力被带到这里」的发现 |
| 底线 | **断开网络也能走完前三屏**，全程不出现报错、不出现转圈卡死 |
| 可复述 | 离场后能对别人说出一句话：它给消费陷阱命名 |
| 可带走 | 评委手里留下一张卡片（截图带框 + 陷阱清单 + 一句结论） |

---

## 3. 产品边界

**做**
- 对手机截图里的消费陷阱做识别、框选、命名、解释
- 三段预设示例的引导式体验（教 → 练 → 放手）
- 用户自己的截图（拍照 / 相册 / 选示例）
- 一张可转发的卡片

**不做（明确砍掉，理由见括号）**
- 登录、注册、支付、订阅、后台管理（对拿奖零贡献，只增加演示出错点）
- 多语言（只做中文；省下的时间给文案打磨）
- 真实 App 注入 / 浏览器扩展 / 自动截图（技术风险高、现场不可控）
- 碳积分、减碳量等未经验证的量化（容易被评委质疑，且偏离「揭示机制」的主线）
- Looply 式三态模拟信息流（原网页版的演示模块，复刻成本最高、对主流程非必需）

---

## 4. 演示主路径（四屏 journey）

| 屏 | 内容 | 交互 | 评委心里 |
|---|---|---|---|
| ① 教 · 示例 A | 一张电商截图 | **入口在图上**：点中消费陷阱 → 该处被框住 + 底部卡片上线（陷阱名 + 一句机制解释）；没点中时卡片只是虚线空壳。编号跟「找到的先后」走，第一个点中的就是 ① | 原来这叫陷阱 |
| ② 练 · 示例 B | 支付确认页截图 | **不再预制**：两处（默认勾选的保障服务、自动续费的省钱月卡）都要自己在图上点出来（「这张图里有 2 处，你来找找看」）；点错不判错 | 我好像会了 |
| ③ 放手 · 示例 C | 红包活动页截图 | **一处都不预先揭示**，全靠自己找（「直接显示」兜底） | 开始主动观察 |
| ④ 用 · 你的截图 | 用户自己的截图（或从示例里选） | 云函数识别 → 逐个揭示 → 生成卡片 | 用刚学会的语言看自己的手机 |

**三档角色写进数据里**（`mode` 字段），改它等于改演示脚本：

| mode | 进屏时预先揭示 | 卡片上 |
|---|---|---|
| `reveal`（教） | 无 | 一直写着陷阱名，等你点 |
| `find-one`（练） | **第一处** | 没找到的那几张**藏起名字**，只写「第 N 处 · 还没找到」 |
| `find-all`（放手） | 无 | 同上 |

> 找的模式里，**卡片不能剧透名字**，也不能靠一路点卡片把「找」绕过去 —— 没找到的卡片点不动，只给一句「先在图里找找看」。
> 用户在图上点中未找到的区域时，靠的是一层**透明热区**（看得见就没有「找」的意义了），所以同一张图的热区不能重叠，这条由 `tests/check-shots.js` 自动守着。

**关键结构约束**

1. **底部卡片是唯一入口**，不设隐藏热区让用户猜（猜不到就是挫败）。
2. **前三屏数据本地写死、不联网** —— 演示风险被隔离在前三屏之外，只有第四屏依赖网络与模型。
3. **允许左右滑动回看**，配进度提示；控制权交给用户。
4. **第四屏永远给两条退路**：「用自己的图」与「从示例里选」，任何一条都能走通。
5. **「按住，只看商品」是全局常驻按钮**，不是某一屏的附属功能。

---

## 5. 功能需求

### P0 · 必做

| 编号 | 功能 | 验收标准 |
|---|---|---|
| F1 | 三段示例 journey，图卡联动，一图多点逐个揭示 | 每段示例的每个陷阱都能被单独揭示，框只框住对应元素 |
| F2 | 「用户先找，再揭示」 | 提示出现后，用户点中未揭示区域 → 该区域被框住并出现解释；点错不判错，可直接点「直接显示」 |
| F3 | **可转发的卡片** | 生成一张卡片（截图带框 + 陷阱名清单 + 一句结论），可分享给微信好友 |
| F4 | 逐屏体验与进度自洽 | 进度指示能同时表达「第几张图」与「图内第几个」，不自相矛盾 |

> **F3 实现时定下的三件事：**
>
> 1. **「一句结论」跟着维度走，不跟着单个陷阱走。** 只命中一个维度就用那个维度的收尾话（写在词典里，5 个维度各一句）；命中两个以上，说明这一屏的证据已经够说明一件共同的事，就用那句总的（「这几处都在做同一件事：缩短你从「看到」到「决定」的时间。」）。收到某一个陷阱上就变成复述了，用户读完还得自己想它们有什么关系。
> 2. **排版的逻辑和画布彻底分开**（`utils/card.js` 出计划、`pages/card/` 照着画）。理由是 canvas 只在真机上跑，不切开的话卡片的排版一个测试都写不了。
> 3. **主按钮是转发，存相册是次要动作。** 转发不需要任何授权，存相册会弹系统授权 —— 阶段 9 的检查清单里写着「无授权弹窗」，这条设计就是为它让路的。
>
> 另外：**卡片只展示这一屏里已经标出来的那几处**。没找到的不进卡片 —— 和「按住」同一条规矩，否则一路点过来就能拿到自己没找过的答案。

### P1 · 亮点

| 编号 | 功能 | 验收标准 |
|---|---|---|
| F5 | 按住「只看商品」 | 按住时已识别的促销元素淡出，商品与当前价格保留；**图上保持干净，没有任何说明文字**；松手还原 |

> 这是全场唯一有「身体参与感」的动作，也是演示高潮（见 §10）。
>
> **实现时定死了两条边界（否则它会和「找」打架）：**
>
> 1. **只盖「已经标出来的」那几处。** 这张图里还没被找到的，按住时也不能消失 —— 否则按住一次就等于拿到答案，「找」这件事被绕过去了。换句话说：**按住是「复习已经看见的」，不是「揭示还没看见的」。**
> 2. **按住不是「揭示答案」的按钮。** 它一度被和第四屏的「揭晓」混为一谈。现在两者彻底分开：按住只负责让**已命名**的东西消失，命名永远由卡片或点击完成。
>
> 原设计在图上压一条「点破差异」的横幅（「刚才先跳出来的是……」），**2026-09-26 按用户实测反馈拆掉**：每次按住都出现，重复且打断看图。现在按住时图上不配任何文字 —— 话都在按钮和引导条里说完了，`tests/check-hold.js` 有断言守着「别悄悄加回来」。

### P2 · 有余力才做

| 编号 | 功能 | 说明 |
|---|---|---|
| F6 | 「你原本打算买什么？」 | 可跳过。用户输入几个字（如「一个杯子」）后，解释能接上原始需求。**演示时只用口播，不让评委当场打字** |
| F7 | 「这条不准确」 | 用户可反馈「框的位置不对」或「解释不符合」。UI 上只留一个小入口，但**答辩稿里必须讲**：产品能指出设计机制，不能仅凭截图断言欺骗 |

### 明确不做

- 报告列表页、历史记录、多图批量（P0 卡片产出已满足「可带走」，列表是纯消耗）
- 用户账号体系、积分、排行榜

---

## 6. 消费陷阱词典（唯一来源）

**唯一来源原则**：全站只有一个词典文件，示例数据、卡片文案、云函数 prompt 全部读它，不允许任何地方重复定义。

### 5 个一级维度 × 13 个模式

> ⚠️ **这张表是 `utils/hooks.js` 的镜像，不是第二个来源。**
> 改词典要**先改代码再改这里**，`tests/check-docs.js` 会逐条比对 id、名字和机制句，
> 对不上就变红。加这条检查的原因很实际：这张表在本 PRD 写完之后一天内就漂了三处
> （C 维度的名字、D1 的机制句、E2 的名字）—— 同一件事写两遍，就一定会有一遍忘了改。

| 维度 | 模式 | 界面上的例子 | 一句话机制 |
|---|---|---|---|
| **A 制造紧迫** | A1 限时倒计时 | 限时优惠、00:14:59 | 用即将结束的提示，压缩比较和考虑的时间 |
| | A2 库存与人气告急 | 仅剩 2 件、X 人正在浏览 | 用稀缺感把犹豫变成抢购 |
| **B 价格感知操纵** | B1 划线价锚定 | ¥99 旁边一个划掉的 ¥299 | 用更高的划线价作参照，让当前价格显得更划算 |
| | B2 先涨后降 | 假折扣 | 折前价被人为抬高，折扣幅度失真 |
| | B3 满减门槛 | 满 199 减 30 | 把优惠设在更高金额，推动额外加购 |
| **C 阻力消除与默认捆绑** | C1 一键下单 | 无确认、免密支付 | 让冲动几乎零阻力地变成订单 |
| | C2 默认勾选与搭售 | 默认加购延保/会员 | 用默认值替你做了决定 |
| | C3 凑单加购推荐 | 再买 ¥21 可减 ¥30 | 用一个差一点就够的门槛，让你再挑一件 |
| **D 社交与从众** | D1 从众提示 | 已有 2000 人购买、大家都在买 | 用别人的选择，降低你的犹豫 |
| | D2 种草与达人同款 | 内容平台导购 | 把推荐包装成生活分享 |
| **E 进出不对称** | E1 取消与退订设障 | 取消会员要 5 层菜单 | 进来只要一步，出去要六步 |
| | E2 自动续费默认开启 | 默认续费 | 让你在忘记的时候继续付费 |
| | E3 权益即将过期 | 优惠券、积分到期 | 用即将失去的东西催你回来消费 |

### 5 个维度的收尾话（卡片底部那句结论用它）

| 维度 | 收尾话 |
|---|---|
| A 制造紧迫 | 它不让你比较，只让你别错过。 |
| B 价格感知操纵 | 它给的是参照，不是折扣。 |
| C 阻力消除与默认捆绑 | 它把「多买一点」做成了不费力的事。 |
| D 社交与从众 | 它把别人的选择，铺在你的选择前面。 |
| E 进出不对称 | 它让进来只要一步，出去要好几步。 |
| **两个以上维度同时出现** | 这几处都在做同一件事：缩短你从「看到」到「决定」的时间。 |

收尾话一律以「它」开头 —— 主语是机制本身，不是用户。这样读起来是在讲这套设计怎么起作用，
不是在教人该怎么想。`tests/check-hooks.js` 里有一条断言守着这个开头。

> **E 维度是本项目最强的可视化素材**：左边「下单 1 步」、右边「取消会员 6 步，其中 3 步是挽留弹窗」。它精准命中绿色消费的价值主张，且比任何 AI 功能都更容易被记住。**建议做成一张对比图，放进演示与卡片。**

### 与旧词典（HookLens 注意力向）的对应

| 旧模式 | 新归属 |
|---|---|
| E1 假性紧迫 | → A1 |
| A2 摩擦移除 | → C1 |
| B1 随机多变酬赏 | → C3（抽奖/凑单同属可变门槛逻辑） |
| C2 人造错失恐惧 | → A1 / A2 |
| C3 社交认可量化 | → D1 |
| D1 劳力与资产累积 | → E3 |
| D2 退出内疚 | → E1 |

旧 A1 停顿线缺失、C1 社交亏欠、B2 悬念式微触发在消费向里没有合适落点，**弃用**。新增 B2、C2、D2、E2 四个消费特有模式。

---

## 7. 数据模型

```
HookPattern   { id, dimensionId, name, note, mechanism }      // 词典条目，唯一来源

Shot          { id, appName, image, hooks: Hook[] }            // 示例截图（本地写死）
  Hook        { patternId, title, note, rect: {x,y,w,h}, order }

Annotation    { id, patternId, title, note, rect }             // 揭示结果（用户/AI 产出）
                                                                 // rect 统一为 0–1 归一化坐标

Card          { id, imageFileId, annotations, summary, createdAt, shareToken }
```

**坐标一律使用相对图片的 0–1 归一化矩形**（左上原点）。这是跨设备不错位的唯一办法，也是网页版验证过的方案，直接沿用。

---

## 8. 页面与技术方案

### 页面清单

| 页面 | 作用 | 数据 |
|---|---|---|
| `pages/journey/` | 三段示例的引导式体验（数据驱动，一套模板跑三屏） | 本地写死 |
| `pages/analyze/` | 用户自己的截图：选图 → 识别 → 逐个揭示 | 云函数 + 云存储 |
| `pages/card/` | 卡片产出与分享 | 云数据库 |

> 首页即 `journey`，**不要独立的首页**（手册：打开即是内容）。顶部不放 tabbar——三个页面是流程关系，不是并列关系。

### 云开发设计

```
用户选图 → wx.compressImage 压缩 → wx.cloud.uploadFile 传云存储
        → 云函数 detect(fileID) → 视觉模型分类 → annotations[]
        → 前端逐个揭示 → 云函数生成卡片 → 云数据库存档
```

- **密钥不需要**：走**云开发内置大模型**（`@cloudbase/node-sdk` 的 `app.ai()`，`createModel('cloudbase')`），
  模型由平台托管，代码和仓库里没有 Key。
  > 曾考虑接入第三方自定义提供商（阿里云百炼），但**接入第三方要升级标准版**，暂不采用；
  > 代码里保留了 `PROVIDER` 常量，以后升级了改一行就能切回去。
- **必须选支持图片输入的模型**：**已定 `glm-5.3-flash`**（2026-09-22，控制台「生文模型 → 视觉理解」筛选出的
  glm-5.3-flash / kimi-k3 / kimi-k2.8-preview 里最便宜最快的；环境 `cloud1`，模型开关需在控制台打开）。
  ⚠️ 纯文本模型（含**免费的混元 hy3**）传图片会被忽略或直接报错 ——
  它在界面上的表现是「云函数成功返回，但一条标注都没有」，极易被误判成「模型看得出这张图没问题」。
  调试时如果发现「怎么都认不出来」，先看模型是不是选错了。
- **云函数超时默认 20 秒不够**（官方建议视觉调用 60–120 秒），`cloudfunctions/detect/config.json` 里已设 60 秒。
  这条也是「预热缓存」值得做的原因：现场那一屏不该等模型。
- **不要把大图 base64 塞进云函数入参**：走云存储 + fileID，云函数内换临时链接。
- 云函数目录不能有空目录（会导致打包失败 `100129`）。
- **云函数只由一个人负责部署**（部署是「谁上传谁生效」，两人同时改会互相覆盖）。

### 四层防御（演示不能挂）

```
第 1 层 严格校验   词典白名单 + 结构校验 + 置信度过滤（在客户端，见下）
      ↓ 一条都不合格
第 2 层 自动重试   再问一次模型（很多问题是瞬时的）
      ↓ 还是不行
第 3 层 诚实失败   返回空标注 + 明确的失败原因，**不伪造坐标**
      ↓ 连这条路都炸
第 4 层 外层捕获   捕获所有未预料情况，仍返回结构合法的结果
```

> ⚠️ **第 3 层与本文档初稿不同，这一处是改过的。** 初稿写的是「返回一份内置的示例数据」——
> 那是网页版的写法，用到第四屏会出事：**示例数据的坐标是给示例那张图的**，套到用户自己的截图上，
> 框会落在完全无关的地方。那不是兜底，是**假装成功**，比直接失败更糟：用户会以为模型看错了图，
> 而不是以为这次没读出来 —— 「没读出来」还能重试，看错了图只会让人不再信任整个产品。
>
> 第四屏真正的兜底是**预热缓存**（这张图之前成功过，坐标是真的）；缓存不命中就诚实地说没读出来，
> 把「重试 / 换一张」两条路交给界面。理由同 §9「错误处理」：假装成功比直接报错更糟。
>
> **职责切分**：云函数只判**形状与白名单**（它 `require` 不到 `utils/`，不该有第二份校验实现），
> **严格校验在客户端**（`utils/detect-parse.js`，唯一实现）。两边认的字段名、以及对「有没有一条
> 能用的」的判断是否一致，由 `tests/check-detect-defense.js` 里的契约断言守着。
> 已知边界一条：置信度过滤只在客户端生效 —— 模型只报了低置信度结果时，界面要说「没把握」，
> 不能说「没问题」。

> 原网页版的 `detect` 接口是**硬失败**（没 key 直接报错，模型返回不合格也报错），本版必须补上后两层。摘要生成沿用「模板必出 + LLM 可选润色」的两层结构。

---

## 9. 交互与文案规范

### 三条硬规矩

1. **只说机制，不说教。** 每条解读必须能回答「它怎么起作用」，不能回答「它多坏」。
   - ✅「用更高的划线价作参照，让当前价格显得更划算」
   - ❌「商家套路太深了，别上当」
2. **优先用小程序自带组件**（`view` `text` `button` `image` `swiper` `scroll-view` `icon`）。官方已提供的「行为」（点击热区、滚动、无障碍）不要自己重做。
3. **不引第三方 UI 库**（Vant / TDesign / uView）。它们默认的配色和圆角会与规范冲突。

### 视觉与文案

- 视觉气质：朴素、克制，接近小程序原生（`"style": "v2"` 的 WeUI 2.0 观感）。**不要急着覆盖它。**
- 截图是每一屏的绝对主视觉，界面元素越少越好。
- 卡片标题 = 陷阱名（这是记忆点的落点）；可加一个极轻的分类标签（如「制造紧迫 · 价格感知」），让四个名字之间产生套系感，为最后的方法论收束留下依据。

### 错误处理

- **错误不许吞掉**：不允许 `try { ... } catch (e) { /* 空 */ }`。
- 必须做三件事：① 映射成用户能行动的提示（不说 `errCode: -1`，要说「这张图没传上去，检查一下网络再试」）② 原始错误 `console.error` 保留现场 ③ 界面有独立的失败状态，**不要用「加载中」一直转圈掩盖失败**。
- 用户看到的界面状态必须和真实状态一致。假装成功比直接报错更糟。

### 页面守卫

任何依赖「上一步数据」的页面必须先判空再跳转，`redirectTo` 后面**别忘了 `return`**。

---

## 10. 演示脚本（90 秒）

| 时间 | 动作 | 备注 |
|---|---|---|
| 0–5s | 打开即示例 A 屏 | 无登录、无引导页 |
| 5–35s | 点图上的消费陷阱 → 框出现 + 卡片上线讲解；左右切换到第 2、3 个陷阱 | 每个陷阱 8–10 秒，不要念稿 |
| 35–55s | 进入示例 C：「还有一处，你觉得在哪？」→ 请评委点 | 全场唯一让评委主动操作的一刻 |
| 55–70s | **按住「只看商品」** → 促销提示消失（图上保持干净）→ 松手还原 | **演示高潮：唯一有身体参与感的动作** |
| 70–85s | 换成评委的截图（现场走「从示例中选择」），生成卡片并转发 | 提前在自己的手机里备一张图 |
| 85–90s | 收束：「5 个维度、13 种模式，任何一个 App 都能这样读一遍」 | 收束成方法论，不是工具 |

**排练要求**：完整走 5 遍；**其中一遍必须断网排练**（验证前三屏不受影响）；另需准备一张手机截图作为「评委的截图」的兜底素材。

---

## 11. 验收标准

判断标准要能自动成立，**不要写字面数字**（加一个文件，数字就过期了）。

- [ ] `node tests/run-all.js` 的**每一行末尾都显示 `0 失败`**
- [ ] 汇总脚本能**自动发现** `tests/check-*.js`，不硬编码文件清单
- [ ] **断开网络**，走完前三屏，界面不出现任何报错或永久转圈
- [ ] 故意让云函数返回非法数据，界面仍能出结果（走兜底），且能在日志里看到原始错误
- [ ] 缩放/不同机型下，标注框仍框在正确的元素上（归一化坐标生效）
- [ ] 手机上（不插数据线）完整走一遍，不依赖开发者工具的模拟器
- [ ] 全程无登录、无授权弹窗、无引导页
- [ ] 卡片能真正转发给自己并打开

---

## 12. 风险与对策

| 风险 | 对策 |
|---|---|
| 现场网络差，第四屏卡住 | 前三屏完全离线；第四屏有「从示例中选择」；云函数四层兜底 |
| 视觉模型识别不准 | 演示时不解释准确率，只强调「命名」；提供「这条不准确」入口 |
| 评委质疑「你怎么知道这是欺骗」 | 答辩明确：只判断设计机制，不判断事实真伪（F7） |
| 示例截图涉及真实 App 版权 | 使用重构的示意截图，不直接截真实 App（原网页版已定此原则） |
| 时间不够、做不完 | P0 先出闭环（示例 journey + 卡片），P1/P2 有余力再加 |

---

## 13. 待定

- **产品名**：**还没定。** 代码里暂时用「收手吧」占位（工作名「看清消费」→「先看清」→「空白的网页」均已改掉；当前占位名由用户指定）。
  ⚠️ **AppID 已经拿到，但小程序名还没取 —— 这两件事是分开的。** 别因为有了 AppID 就以为名字定了（本次差点因此写错三份文档）。
  定名之后：`utils/brand.js` 和 `app.json` 必须和注册名**一字不差** —— 小程序名称全平台唯一、注册时写死，微信里给用户显示的就是它。
  改名成本：改代码是 0 成本；改**注册名**要消耗每年 2 次机会之一（发布前 2 次，发布后 2 次/年）。**所以最好在发布前就把名字敲定。**
- **演示时长与评审维度**：尚未拿到评分表。拿不到就按「现场参与感」最大化设计（即当前方案）。
- **F7「这条不准确」的实际形态**：先做最小入口（两个选项的小弹层），不做完整反馈闭环。
