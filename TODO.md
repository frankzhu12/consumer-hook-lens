# TODO · 收手吧（小程序）

配套文档：[PRD.md](PRD.md)

## 纪律（先读这三条，再往下做）

1. **一次只做一步。** 每一步都写清「做完我该看到什么」；看不到就不算做完。红着不许往下走。
2. **有界面的手点，没界面的必须写脚本。** 「我点了一遍，没问题」对云函数、算分、文案生成这类看不见的东西**不算验证**。
3. **判断标准要能自动成立。** 不写「一共 393 项检查」，写「每一行末尾都显示 `0 失败`」。

---

## 阶段 0 · 准备（不用写代码，但缺一样就动不了）

- [x] 注册小程序账号（`mp.weixin.qq.com` → 选「小程序」→ 个人主体），拿到 **AppID**（`wx` 开头）
      > 已完成。AppID 已填进 `project.config.json`（按项目纪律，**文档里不复述它的值**，要看就去看那个文件）。
      > 备案和微信认证不用做，只影响「正式发布给所有人用」；开发版、体验版、真机预览都不受影响。
      > ⚠️ **拿到 AppID ≠ 名字已经定了** —— 这两件事是分开的，本次差点因此写错三份文档。
- [ ] **定下小程序名，并在后台注册上去** —— 代码里现在用「收手吧」占位
      > 名字**全平台唯一**，注册时写死，微信里给用户显示的就是它。定下来之后 `utils/brand.js` 和 `app.json` 必须和它**一字不差**。
      > 改名有配额：**发布前 2 次、发布后 2 次/年**。所以最好在发布前就敲定，别留到演示当天。
      > 不阻塞开发 —— 两个文件各改一处、跑一遍测试即可；`check-brand.js` 会盯着别处不许漏出第三个名字。
- [ ] 装**微信开发者工具 Stable Build**，扫码登录
- [ ] 装 **Node.js LTS**（跑测试用）
- [x] 开通**云开发**，记下**环境 ID** —— 已完成
      > 环境 ID 不是密钥，写在客户端是正常的；用 `wx.cloud.init({ env })` 指定，别写进文档（会漂）。
- [x] 准备**模型 API Key** —— **已经不需要了**
      > 改走**云开发内置大模型**，Key 由平台托管、不暴露在前端。见阶段 7 开头。
- [x] 准备 **3 组示例截图素材** —— 已交付，放在 `assets/samples/`（862×1824 JPEG 共约 1MB，能进主包）
      - `01.jpg` 电商商品页（购物）→ **A1 限时倒计时 + B1 划线价锚定**（用于「逐个揭示」）
      - `02.jpg` 支付确认页（支付）→ **C2 默认勾选与搭售 + E2 自动续费默认开启**（用于「先找再揭示」）
      - `03.jpg` 红包活动页（活动）→ **A1 限时倒计时 + D1 从众提示**（A1 在两种场景复现，正好印证「任何 App 都能这样读一遍」）
- [ ] **E1 进出不对称的对比素材还没做**（下单 1 步 vs 取消 6 步）。最强可视化素材，但不在前三屏主流程里，可放在卡片或答辩页上，不阻塞开发
- [ ] 演示备用：在自己手机里存一张真实的购物截图（当评委不传图时用）

---

## 阶段 1 · 骨架

- [x] 建项目，`app.json` 只留一个页面，标题填产品名
- [x] `"style": "v2"` 保持不变（这是设计规范，别急着覆盖）

**做完该看到什么**：模拟器打开就是白底，顶部标题正确，没有任何报错。

---

## 阶段 2 · 词典与纯逻辑 + 测试

> 这一步全是**没有界面**的东西，所以必须写脚本。这里的测试是整个项目的地基，后面所有步骤都靠它兜底。

- [x] `utils/hooks.js` 写入词典：5 个维度 + 13 个模式（照 PRD §6）
- [x] `utils/hooks.js` 提供：按 id 取模式、按维度取模式、非法 id 返回 undefined
- [x] `utils/reveal.js` 纯逻辑：给定示例数据和已揭示集合，算出「下一个未揭示的」「是否已全部揭示」「进度显示文本（第几张图 + 图内第几个）」
- [x] `utils/annotations.js`：归一化矩形校验（clamp 到 0–1、最小边长、越界裁剪）
- [x] `tests/check-hooks.js`、`tests/check-reveal.js`、`tests/check-annotations.js`
      > 这里本来写着「共 61 项」——**不该写数字**。项目往后长，这个数会一直变，改一次忘一次就变成假信息。
      > 判断标准只有一个：每一行末尾都是 `0 失败`。
- [x] `tests/run-all.js` **扫描目录自动发现** `tests/check-*.js`
      > ❌ 不要在汇总脚本里硬编码文件清单。新人加了测试但忘了加进清单，汇总会显示全绿而那个套件根本没跑 —— **一个没跑的绿色检查，比没有检查更危险**。
      > ✅ 阶段 6 加 `check-page.js` 时验证过这条真的生效：没改任何清单，跑汇总就自动多出一行「发现 6 个套件」。

**做完该看到什么**：`node tests/run-all.js` 每一行末尾都是 `0 失败`；故意把一个断言改错，能看到它变红。

> ✅ 已做到。首跑就抓出两个问题：① `annotations.js` 浮点精度 bug（`0.8 + 0.2` 裁剪后算出 `0.19999999999999996`）② 我自己的断言写错了（模型会把坐标返回成数字字符串，接受它是刻意的容错）。修完 3 个套件全绿。

---

## 阶段 3 · 示例 journey 第一屏

- [x] `pages/journey/` 数据驱动：从 `data/shots.js` 读示例 A
- [x] 截图占满主视觉，底部一张**可点卡片**（陷阱名 + 一句机制）
- [x] 点卡片 → 图上对应区域出现高亮框 + 卡片展开解释

**做完该看到什么**：手点卡片，框准确套在**那一个**元素上（不是整块区域）；再点一次可以收起。

> ✅ 已做到。三段示例共 6 处，坐标不是估的：用 `sips` 按估算值裁出条带逐一核对后修正（例如倒计时条实际在 0.544–0.623，比估算低了 0.007）。`tests/check-shots.js` 把坐标合法性、图片文件是否存在、id 是否在词典里全部纳入自动检查。

---

## 阶段 4 · 一图多点 + 逐个切换 + 进度自洽

- [x] 卡片左右滑动，切换到同一张图的第 2、3 个陷阱，图上高亮跟着切换
      > 实现时做了一个调整：**滑动只换卡片，点卡片才揭示**。理由是保住「一次点击 = 一次命名」的节奏 —— 滑一下框就自动冒出来，会把「揭晓」这个动作稀释掉。
- [x] 反向：点图上已识别区域，切换到对应解释
- [x] 进度提示同时表达「第几张图」和「图内第几个」，不互相矛盾
      > 原型里 01–03 是 `1/3`、`2/3`，04–05 变成 `1/2`、`2/2`，同一个位置出现两套进度，看起来像数字在乱跳。演示时评委会盯着这串数字。
      > 解决：外层「示例 1 / 3」和总量提示「这张图里有 2 处」放**顶部**，内层「已标出 1 / 2」放**底部卡片下方** —— 物理隔开，不会再被读成一套。
- [x] 卡片上方给一行极轻的总量提示（「这张图里有 2 处」）

**做完该看到什么**：左右滑动能跑完示例 A 的所有陷阱；进度数字自洽；「还有更多」这件事一眼可见。

---

## 阶段 5 · 用户先找，再揭示

- [x] 示例 B 加载后，只揭示第一处（C3 凑单门槛）
- [x] 卡片提示「还有一处，你觉得在哪？」
- [x] 用户点中未揭示区域 → 该区域被框住 + 出现解释
- [x] 点错**不判错**：给一句轻提示（「看看这里的作用」），可继续找
- [x] 提供「直接显示」按钮，不想找也能走通
- [x] 全部揭示后，把已揭示的框**同时带 1、2 编号显示**（把「我学会了」可视化）
- [x] **顺带补上**：把页面在 Node 里跑起来测（`tests/check-page.js`）
      > 阶段 6 需要它（「连续按 10 次不出现状态错乱」是状态机，属于没界面的东西）。做法是把 `Page` 和 `wx` 两个全局桩掉 —— 小程序页面说到底就是一个普通对象。

> ✅ 已做到。实现时定下三件事：
> 1. **三档角色写进数据**（`mode` = `reveal` / `find-one` / `find-all`），三段依次是教 → 练 → 放手。**示例 C 改成一处都不预先揭示**（原计划是"只揭示一处"），否则它和示例 B 完全重复，"放手"这两个字就落不了地。有「直接显示」兜底，不会卡住。
> 2. **找的模式下卡片不剧透名字**：没找到的那几张只写「第 N 处 · 还没找到」，而且点不动（一路点卡片就把「找」绕过去了）。点它只给一句「先在图里找找看」。
> 3. 图上未找到的地方铺一层**透明热区**接点击 —— 看得见就没有「找」的意义。所以同图热区不能重叠，`tests/check-shots.js` 自动守着这条。

**做完该看到什么**：不看任何提示也能靠点击找到第二处；点错不卡住；揭示完能看到两个编号框同时存在。

---

## 阶段 6 · 按住「只看商品」（演示高潮）

- [x] 全局常驻按钮（**不是**某一屏专属）
- [x] 按住时：已识别的促销元素淡出，商品图与当前价格保留
- [x] ~~屏幕上出现一句点破差异的话~~（**2026-09-26 拆掉**：用户实测「刚才先跳出来的是……」这条横幅意义不大，
      按住时图上保持干净 —— 盖住就是盖住，话都在按钮和引导条里说完了）
      > 原设计认为只「灰掉」不够、要给差异起名字；实测下来这句横幅每次按住都出现，
      > 重复且打断看图。`tests/check-hold.js` 有断言守着「别悄悄加回来」。
- [x] 松手立即还原；按住过程中不触发误点

> ✅ 已做到。四处需要说明的取舍：
>
> 1. **盖子是实心的，不是「变灰」。** 用页面底色 + 虚线边盖住那一块 —— 它要替你说出「这里本来有东西」。变灰只是把注意力挪走，盖掉才是让东西消失。所以 `B1` 只框划线价那一小段：按住时 `¥299` 没了、`¥99` 还在，**那个对比才是这个陷阱被读懂的一刻**。
> 2. **按钮下面那行小字只负责邀请动作**（「按住下面，把刚才那几处拿掉」）。按住时它闭嘴 —— 图上保持干净，一个字不多说（横幅已拆）。
> 3. **用 `bindtouchstart`/`bindtouchend`，不用 `bindlongpress`** —— 后者只在长按时触发一次，不含手指刚碰到的那一刻，松手时机也无从得知，而「按住→松开」的连续性正是这个动作的全部。
>
> 按钮文案：没按住是「按住，只看商品」，按住时变「松开，恢复」。一处都没标出时按钮是灰的，按下去给一句「先标出一处，再按住试试」。

**做完该看到什么**：按住 → 促销提示消失且图上干净无字 → 松手完全还原；连续按 10 次不出现状态错乱。

> ✅ 已做到，且**那 10 次不是用手点的**。`tests/check-page.js` 把页面在 Node 里跑起来（桩掉 `Page` 和 `wx` 两个全局），逐字段比对「每一次松手后的数据是否回到按住之前」。手点看的是「好像没坏」，脚本比的是「哪个字段变了」—— 后者才挡得住「第三屏按住之后盖子留在图上」。
>
> 顺手抓出一个老 bug：`top:54.400000000000006%` 写进了内联样式。原因是先取整到 4 位小数、再乘 100，浮点误差又回来了。改成先乘再取整到两位，并加了「0 到 1 逐千分位扫一遍」的断言。
>
> 另外把 `B1` 的坐标重新量了一遍：原来那条框右边界切掉了最后一个「9」。现在边界是按原图裁出来核对的（`¥299` 占原图 x 187–268、y 858–881），6 处坐标全部逐块裁图确认过。

---

## 阶段 8 · 卡片产出与分享

- [x] 生成卡片：截图带框 + 陷阱名清单 + 一句结论
- [x] `onShareAppMessage` 可转发给自己并打开
- [ ] 卡片存档到云数据库（最小字段即可，不做列表页）
      > **刻意往后放**：PRD 明确不做列表页，那存进数据库就没有任何用户看得见的地方。它对答辩可能有点用（「数据落在哪」），但它同时也多一个失败点。等云开发真的接起来（阶段 7）再顺手加，代价是几行代码。

> ✅ 已做到。三个需要说明的取舍：
>
> 1. **排版和画布彻底切开。** `utils/card.js` 只产出**一份绘制计划**（一串带绝对坐标的图元 + 画布尺寸），`pages/card/` 拿到它只管照着画、不做任何判断。
>    这么切的理由是：**canvas 只在真机上跑得起来，Node 里没有。** 如果「算位置」和「调 canvas API」写在一个函数里，卡片的排版就一个测试都写不了，只能靠上真机肉眼看。
>    切开之后，「折行对不对、有没有出界、编号和列表对不对得上、结论跟不跟着维度走」全都能断言（`check-card.js`）。真机上只剩「字有没有画歪」要看。
> 2. **折行是自己写的，规矩是「宁可估宽，不要估窄」。** canvas 里虽然有 `measureText`，但排版要在没有 canvas 的地方也能算，所以按字符估宽：汉字一个字宽、拉丁 0.6 个字宽。估宽了顶多这一行少放两个字，估窄了字就画出去了。还带了一套中文禁则（行首不能是「。，、；：」这类，行尾不能是「（「」）。
>    > 有一条断言是「折行拼回去必须等于原文」—— 它一次就抓到了我折行时把当前那个字丢掉的 bug。
> 3. **转发不需要任何授权，所以它是主按钮；存相册会弹系统授权，所以它只做次要动作。** 阶段 9 的检查清单里写着「无授权弹窗」，这条设计就是为它让路的。
>
> 顺带做的两件事：
> - **产品名收成了唯一来源**（`utils/brand.js`），并加了 `check-brand.js` 扫全项目防止漏改 —— 它第一次跑就抓到 `pages/journey/index.json` 里自己盖了一层标题。
> - **页面状态机套件改名为 `check-page.js`**（原来叫 `check-page-hold.js`），因为它现在管的不只是按住，还有卡片入口的开关。

**做完该看到什么**：转发到自己的微信，点开能看到完整卡片。

> ⚠️ **一件只能手点确认的事**：微信在消息列表里给转发卡片显示的缩略图是**顶部 5:4 那一块**。卡片本身接近 1:2（截图很高），所以缩略图里大概只能看到截图的上半部分、不一定看得到标注框。这不影响「点开能看到完整卡片」这条验收，但**转发出去好不好看，必须真机转发一次才知道**。如果不好看，最省事的改法是给 `imageUrl` 单独画一张 5:4 的小图，而不是去改整张卡片的版式。

---

## 阶段 7 · 接自己的截图（唯一依赖网络的一步）

> ✅ **两个前提都过了**：正式 AppID 已就位；**云开发环境已开通**。
> ✅ **不用自备模型 Key**：走**云开发大模型服务**（Key 由平台托管、不进客户端）。
>    要求基础库 ≥3.15.1，本项目 `project.private.config.json` 里是 **3.17.3**，已满足。
>    → 原先那条「把模型 Key 配进云函数环境变量」**作废**，替换成「在云开发控制台启用模型」。
> ⚠️ **模型路线已定（2026-09-22）**：第三方自定义提供商（阿里云百炼等）**要升级标准版** → 暂不采用；
>    走**内置模型**（`createModel('cloudbase')`）。模型 = **`glm-5.3-flash`**（控制台「生文模型 → 视觉理解」
>    三个候选里最便宜最快；环境 ID 已填进 `app.js`，**值只在那一处写**）。
>    ⚠️ 剩余动作：控制台把 glm-5.3-flash 的**状态开关打开**，然后部署 detect 云函数（云端装依赖）→ 真机手点第四屏。
> 注意：**P0 那条线（示例 journey → 揭示 → 卡片）已经闭环，而且完全不依赖网络** —— 阶段 7 卡着不影响先把阶段 9 排练排好。

### 这一节先定下的六件事（别在中途改主意）

1. **不新建 `pages/analyze`。** 让 `journey` 支持**第二数据源**：云函数返回的 `annotations` 映射成 `Shot` 的形状，于是标注层、逐个揭示、「按住只看商品」、卡片生成**全部复用**。
   > 另开页面 = 把标注那一套模板再抄一份。项目最硬的规矩是「一份内容只有一个来源」；抄一份就意味着以后每改一次标注层要改两处，且一定会漏掉一处。
2. **云函数出参必须带 `source`**（`model` / `cache` / `fallback`）。界面靠它决定要不要说那句真话 —— 兜底可以让演示继续，但**不能让界面以为自己成功了**。
3. **宁少不错。** 模型对每处返回置信度，低于阈值直接丢掉。图上只框 2–3 处精准的，比框 6 处杂乱的更像「它真的看懂了」。
4. **失败不许假装。** 兜底时界面明说「这张图没读出来，先用示例看看」+ 重试入口，绝不把兜底数据伪装成识别结果。
5. **prompt 里的词典由脚本生成，不许手抄。** 加断言守「生成物 = `utils/hooks.js`」。做法沿用 `tools/preview-card.js` 内联 `utils/canvas-draw.js` 的先例 —— 那条「必须是同一份」的理由在这里同样成立。
6. **演示素材两手准备。** 预热缓存同时覆盖「预备素材」和几张常见截图；现场优先请评委用自己的图，不顺利就切预备素材 —— 两条路都已经预热过。

### 步骤（一次只做一步）

- [x] **7.1 解析与校验的纯逻辑 + 测试**（离线可验，不碰云环境，先做这个）
      - `utils/detect-parse.js`：原始模型返回 → 合法 `annotations[]`。白名单（`patternId` 必须在词典里）、结构校验、置信度过滤、坐标 0–1 归一化与越界裁剪、数量上限
      - 每处带 **`evidence`（图上原话）**，如「仅剩 2 件」—— 比只给一个陷阱名有说服力
      - 模型返回空数组 / 全被过滤掉时，**返回明确的「没看出问题」而不是空界面**（这也是一种结果）
      - **做完该看到什么**：`node tests/run-all.js` 多出一行 `check-detect-parse`、末尾 `0 失败`；喂 `{}`、坐标越界、`patternId` 不在词典、空数组、模型胡说的字段，都给确定结果而不抛异常
      > ✅ 已完成：`utils/detect-parse.js` + `tests/check-detect-parse.js`（43 项）。落定时多定了三条，都写进了代码注释：
      > **① 同一模式只留把握更高的一处** —— 界面的「已揭示」是按模式 id 记的，两条同 id 只能同进同出，留着它们「点一下揭示一处」直接失效；
      > **② 模型没给置信度时不替它决定** —— 拿不到信息就丢，会把一整份能用的结果变成空；
      > **③ `shapeOk` 分开「没看出问题」和「这次没成功」** —— 前者是合法结论，后者才去走重试与兜底，两者在界面上长得不一样。

- [x] **7.2 词典同步到云函数**（一份来源）
      - `tools/build-detect-prompt.js`：从 `utils/hooks.js` 生成云函数要用的词典与 prompt 片段
      - `tests/check-detect-sync.js`：改词典忘了重新生成就变红
      - **做完该看到什么**：手动改一下 `utils/hooks.js` 的某个名字、不重新生成，测试变红；重新生成后回到全绿
      > ✅ 已完成：`tools/build-detect-prompt.js` + `cloudfunctions/detect/lib/hook-dict.js`（生成物）+ `cloudfunctions/detect/lib/prompt.js` + `tests/check-detect-sync.js`（21 项）。
      > **变异验证过**：把词典里一个陷阱名改一个字、不重新生成 → 两条断言立刻变红，输出里直接给出重新生成的命令；还原后回到全绿。
      > prompt 里刻意**不含任何一个陷阱名**，模式清单运行时从 `hook-dict` 拼 —— 于是「手抄漂移」在结构上不可能发生，另有一条断言扫源码守着。
      > 云函数**不能** `require` 上级目录（上传只打包自己那层），这是必须生成本地副本的原因；做法沿用 `tools/preview-card.js` 内联 `utils/canvas-draw.js` 的先例。

- [x] **7.3 云函数 `detect` + 四层防御**
      - 入参**只有 fileID**（不要 base64 大图）；云函数内换临时链接
      - **四层防御全部落地**：严格校验 → 重试一次 → 确定性兜底数据 → 外层 try/catch；**每一层兜底都要在返回里写明 `source`**
      - **做完该看到什么**：故意让模型返回非法数据 → 界面仍出结果且 `source=fallback`，日志里能看到原始错误
      > ✅ 已完成（**第 3 层按下面的修正实现**）：`cloudfunctions/detect/index.js`（薄入口：临时链接 → 模型 → 防御）、
      > `lib/extract-json.js`（从模型文本里抠 JSON）、`lib/defense.js`（四层编排，模型调用与日志注入进来）、
      > `config.json`（超时 60 秒）、`tests/check-detect-defense.js`（56 项）。
      > **离线那一半已验**：喂坏数据 → 重试 → 诚实失败、`source=fallback`、空数组、原始错误进日志；变异验证过（把白名单自检关掉，3 条断言立刻变红）。
      > 「界面上仍出结果」那一半要等 7.4 接上前端才能手点确认。
      >
      > ⚠️ **两处修正（都写进 PRD 了）**：
      > **① 第 3 层不再是「返回内置示例数据」。** 示例数据的坐标是给示例那张图的，套到用户的图上框会落在
      > 完全无关的地方 —— 那不是兜底，是假装成功。改成**诚实失败**：空标注 + 明确原因 + 界面给「重试 / 换一张」。
      > 第四屏真正的兜底是 7.5 的**预热缓存**（坐标是真的）。
      > **② 职责切分：云函数只判形状与白名单，严格校验留在客户端。** 云函数 `require` 不到 `utils/`，
      > 与其在云函数里放第二份校验实现（两份必漂），不如让它只管「看得见」，客户端管「信不信」。
      > 两边认的字段名与判断由**契约断言**守着 —— 这类接缝漏了最难查：云函数说成功、客户端说解析不出来，
      > 界面上只表现为「这张图没什么问题」，看起来像模型的判断。
      > 另外 `tests/check-detect-defense.js` 里记了一条**已知边界**：置信度过滤只在客户端生效。

- [ ] **7.4 前端接入：journey 第二数据源**（拆成三小步，一次只做一步）
  - [x] **7.4a 纯逻辑先落地**（离线可验，不碰界面）
        - `utils/analyze.js`：第四屏的**状态机** + **三种「没有」的文案** + 把云函数返回的标注**映射成 `Shot` 形状**
        - 为什么先做这个：第四屏最容易出错的地方全是「看不见的」—— 拿到结果以后该说什么、
          该进哪个状态。这些判断一旦写在页面事件里，就只能靠手点了；抽成纯逻辑就能逐条喂。
        - **三种「没有」必须说成三句不同的话**（混成一句就把诚实这条规矩丢了）：
          ① 模型说**没看出问题**（`source=model` 且它一条都没报）→「这张图里没看出问题」；
          ② **这次没读出来**（`source=fallback`）→ 那句实话 + 「再试一次」「换一张」两个出口；
          ③ 模型**报了但都被置信度筛掉了** →「有几处它不太确定，先不标了」——不能说「没问题」。
        - **做完该看到什么**：`node tests/run-all.js` 多出一行 `check-analyze`、末尾 `0 失败`；
          四种返回（正常 / 空 / 报了但全被置信度筛掉 / 云函数兜底）各给出确定的文案与状态，
          且**三种「没有」是三句不同的话**（这一条有断言守着）
        > ✅ 已完成：`utils/analyze.js` + `tests/check-analyze.js`（51 项）。
        > 落定时多定了两条：**第四屏复用教学屏的 `reveal` 角色**（名字写在卡片上、点一下看它在哪）——
        > 这样揭示、按住、卡片生成一行都不用改，比新加一个 mode 少一处要跟着改的地方；
        > **映射只搬 `id`/`rect`/`evidence` 三个字段，不重复校验**（白名单和坐标已经在 `utils/detect-parse.js` 判过，
        > 再判一次就是两份规则）。「没读出来」那句标题出自 `detect-parse` 的唯一来源，出口（两个按钮）在这里加。
        > **变异验证过**：把「不太确定」那句话改成「没看出问题」→ 断言立刻变红并打出三句话对照；还原后全绿。
  - [x] **7.4b 界面接入：第四屏与选图**（页面状态机可在 Node 里桩测）
        - 第三屏收尾出现「用你自己的图试一次」—— 这是演示脚本 70–85s 的转折点；**不做独立首页入口**
        - `wx.chooseMedia` 拍照 / 相册 → 先压缩再上传 → `wx.cloud.uploadFile` 拿 fileID
        - 三态渲染：选图 / 读图中（真实进度，不是无限转圈）/ 失败；`wx.cloud.init({ env })` 写在 `app.js` 一处
        - 映射出来的 shot 走**同一套**标注、揭示、按住、卡片模板 —— 不复制模板
        - **「按住，只看商品」在自传图这一屏也要有**，而且它在评委自己的图上比在示例上更有说服力。
          > 阶段 6 的按钮是**全局常驻**的（`utils/hold.js` 是纯逻辑、不绑定哪一屏），接进来只需要把 `revealed` 换成这一屏的即可。别在这里重写一遍。
        - 「从示例中选择」与「用自己的图」并列，两条路都能走通
        - **做完该看到什么**：桩掉 `wx` 跑页面能走通「进第四屏 → 选图 → 出标注」与「云函数失败 → 失败态」；
          **真机手点**那一半要你确认
        > **已完成（2026-09-22）。** 落地：`pages/journey/index.js`（第四屏状态机 + 云链路，钩子视图抽成模块级
        > `buildHookViews`，示例屏与第四屏共用一份拼装；卡片上优先显示图上原话 `evidence`，示例无此字段自然落回词典）、
        > `index.wxml`（选图块 / 失败态块 / 收尾入口；卡片入口行在第四屏整行不渲染，7.4c 再开）、`index.wxss`（三块新样式）、
        > `app.js`（`wx.cloud.init` 一处配置；**环境 ID 是占位符，真机前要换成自己的**）、
        > `tests/check-page-own.js`（90 项：入口门槛、两步真实进度、三种「没有」、失败出口、状态保持、**读图中滑走**）。
        > **变异验证过两处**：拆掉 `applyOwnResult` 的 isOwn 防线 → 竞态 2 条断言变红；拆掉入口的「标完才出现」门槛 → 1 条变红。还原后全绿（16 套件 0 失败）。
        > ⚠️ **剩余动作（2026-09-22 已定案）**：模型 `glm-5.3-flash`、环境 ID 均已写进代码；
        > ① 控制台把 glm-5.3-flash 的**状态开关打开**；② 部署 detect 云函数（云端装依赖）→ 真机手点第四屏。
  - [ ] **7.4c 卡片页与转发**
        - 第四屏也能「做成卡片」：自己的图要能进 `pages/card`（跨页要带会话内的图与标注）
        - **做完该看到什么**：真机上第四屏生成卡片并转发

- [ ] **7.5 预热缓存**（这一阶段最值钱的一条）
      - 识别结果按图片 hash 落云数据库；命中直接返回 `source=cache`
      - 演示前把预备素材与常见截图各跑一遍
      - **做完该看到什么**：**断网**状态下选中已预热的图，仍能出结果

- [ ] **7.6 失败态文案 +「这条不准确」最小入口（F7）**
      - 失败态要说人话（「这张图没传上去，检查一下网络再试」），不出现 `errCode`
      - F7 只做最小入口（两个选项的小弹层），但**答辩稿里必须讲**
      - **做完该看到什么**：拔网线选图，界面给的是可行动的提示 + 重试，不是一个一直转的圈

- [ ] **7.7 提交前扫一遍密钥特征**：`sk-`、`ghp_`、`AKIA`、`-----BEGIN`、`password =`、`secret =`
      > 一旦推到公开仓库，即使立刻删掉，历史记录里还在 —— 只能换密钥。

**做完该看到什么**：真机上选一张图 → 几秒后逐个出现标注框；**把云函数改成返回非法数据，界面仍出结果**（走兜底），且日志里能看到原始错误。

> **为什么这一节值得花这些功夫**：前三屏是「我学会了这套语言」，第四屏是「用这套语言看**我自己的**手机」。
> 评委在自己截图上看到框的那一刻，是他从旁观者变成当事人的唯一时刻 —— 前面所有设计都在为这 70–85 秒铺路。
> 所以这里的取舍标准不是「功能是否完备」，而是**「这一刻必须发生，而且不能翻车」**。

---

## 阶段 9 · 演示排练

- [ ] 完整走 **5 遍**，按 PRD §10 的 90 秒脚本计时
- [ ] **其中一遍断网排练**（现在这条能真正成立了：示例 journey、按住、卡片**全都不依赖网络**）
- [ ] 真机转发一次，确认卡片在微信消息列表里的样子（见阶段 8 那条 ⚠️）
- [ ] 准备备用素材：一张自己的购物截图、一张可以让评委点的示例
- [ ] 检查清单：无登录 / 无授权弹窗 / 无引导页 / 首屏即有内容
      > 「无授权弹窗」这条现在是有设计支撑的：唯一的授权动作（存相册）被放在卡片页的**次要按钮**上，主按钮是转发（不需要任何授权）。
- [ ] 写一段 30 秒口播稿，收尾必须收到方法论上（「5 个维度、13 种模式，任何 App 都能这样读一遍」），**不要讲技术架构**
- [ ] 答辩稿里明确讲一句「这条不准确」的设计意图（只判断设计机制，不判断事实真伪）

**做完该看到什么**：全程零报错；断网那一遍也能把前三屏讲完。

---

## 交付顺序提醒

**P0 已经闭环**：「示例 journey → 揭示 → 卡片 → 转发」这条线完整走通了，而且不依赖网络。它本身就是能拿奖的最小作品。

「按住只看商品」（P1）也已经做完。**注册已完成、正式 AppID 已就位**，阶段 7 现在挡在「开云开发环境 + 配模型 Key」这两件动手的事上，不是挡在等注册。F6/F7（P2）是余力 —— **但即便如此，现在性价比最高的仍然是阶段 9 的排练**：一个不坏的 90 秒，比多一个功能值分。

## 明确不做（别在后面偷偷加回来）

登录、注册、支付、订阅、后台管理、多语言、报告列表页、历史记录、多图批量、碳积分、真实 App 注入。
